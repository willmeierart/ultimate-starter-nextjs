{
  "apps" : [{
    "name": "client",
    "script": "npm --no-automation -- run start",
    "autorestart": true,
    "max_restarts": 25,
    "instances": 4,
    "merge_logs": true,
    "watch": false,
    "env": {
      "NODE_ENV": "production",
      "PORT": 3000
    }
  }]
}
