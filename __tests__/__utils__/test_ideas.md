# Various ideas of things to test for:

- basic:
  - each page renders correctly
  - each page's <head> is unique, correct
  - each link works, prefetches
  - each image has an alt
- complex
  - lifecycle:
    - animation:

