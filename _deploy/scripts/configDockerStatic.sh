# FIRST: make sure backups of all dockerfiles in deployment
# delete any existing dockerfiles and docker compose files out of root
# copy docker-compose.yml out of client/static to root
# run correct commands