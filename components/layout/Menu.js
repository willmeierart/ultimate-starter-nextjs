export default () => {
  // const renderList = (set, element) => {
  //   return set.map((each, i) => (<element key={i}>{ each }</element>))
  // }
  return (
    <div>
      <div>{/* { renderList() } */}</div>
      <style jsx>{``}</style>
    </div>
  )
}
